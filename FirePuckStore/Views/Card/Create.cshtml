@model FirePuckStore.Models.Card
@{
    ViewBag.Title = "Add Card";
}
<header>
    <p class="inlineDisplay">Here you can fill required fields. To upload image click on image area and choose a required image file</p>
</header>

@using (Html.BeginForm("Create", "Card", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Card</legend>
         @Html.Partial("_ImageUploader", Model.ImageUrl)
        <div class="editor-label">            
            @Html.LabelFor(model => model.Category)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Category)
            @Html.ValidationMessageFor(model => model.Category)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.Description)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Description)
            @Html.ValidationMessageFor(model => model.Description)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.PlayerId, "Player")
        </div>
        <div class="editor-field">
            @Html.DropDownListFor(model => model.PlayerId, (IEnumerable<SelectListItem>)ViewBag.PlayerIdList, String.Empty)
            @Html.ValidationMessageFor(model => model.PlayerId)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.Quantity)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Quantity)
            @Html.ValidationMessageFor(model => model.Quantity)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.Price)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Price)
            @Html.ValidationMessageFor(model => model.Price)
        </div>
        <p>
            <button type="submit" value="Create">
                Create</button>
        </p>
    </fieldset>
}
<div>
    @Html.ActionLink("Back to List", "Index")
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script src="/Scripts/ui.selectmenu.js" type="text/javascript"></script>
    <script src="/Scripts/ui.imageselector.js" type="text/javascript"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            $('button').button();
            $('select').selectmenu();
        })
    </script>
}
